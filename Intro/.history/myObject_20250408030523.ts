const User = {
    name: "hemil",
    age: 27,
    isActive: true,
}

function createUser({name: string, isPaid: boolean}) {}

createUser({name: "hemil", isPaid: false})

function createCourse():{}{
    return {}
}