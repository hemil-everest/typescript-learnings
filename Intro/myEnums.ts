const enum Seatchoice{// add const to reduce code generation in javascript
    // AISLE = 10, //default is 0, further now is 11
   AISLE = "aisle", // provide values for all
    MIDDLE = "middle",
    WINDOW = "window",
    FORTH = 0
}

const hcSeat = Seatchoice.AISLE